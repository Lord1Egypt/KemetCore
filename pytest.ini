[pytest]
testpaths = projects
python_files = test_*.py
python_functions = test_*
addopts = -ra --ignore-glob=*/rtl/*
# cocotb RTL testbenches under */rtl/tb run via Verilator (see run_sim.sh), not pytest
