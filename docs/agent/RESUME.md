# Next Step

We are currently in the middle of Phase 4 hardening! We just did AnubisCore's `sha3_256_core` config!
Wait for PR 252 (SethCore P4 remaining) CI to finish, then merge it!
Wait for local hardening of `sha3_256_core` to finish, then open a PR for it!
Update `PROGRESS.md` using `tools/gen_tracking.py` after those merges.
Update `tools/manifest.py` to mark SethCore Phase 4 as `done` and AnubisCore Phase 4 as `done`!

Remember: Use `/goal` mode! Self-merge PRs!
